const game_view = document.addEventListener("DOMContentLoaded", function() {
  const canvas = document.getElementById("game-canvas");
  // canvas.width = 500;
  // canvas.height = 500;
  // const ctx = canvas.getContext("2d");
  // ctx.fillStyle = "#ada8e9";
  // ctx.fillRect(0, 0, 500, 500);

  // ctx.beginPath();
  // ctx.arc(250, 250, 80, 0, 2 * Math.PI, true);
  // ctx.strokeStyle = "yellow";
  // ctx.fillStyle = "blue";
  // ctx.lineWidth = 30;
  // ctx.stroke();
  // ctx.fill();
});

module.exports = { game_view };